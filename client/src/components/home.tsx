import React from "react";
// import { useParams } from "react-router-dom";

const Home = () => {
  // const { userId } = useParams();

  return (
    <div>
      <h2>Just a home page</h2>
    </div>
  );
};

export default Home;
